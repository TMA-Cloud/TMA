{
  "name": "tma-cloud-electron-app",
  "private": true,
  "version": "1.0.8",
  "description": "Desktop client for TMA Cloud",
  "keywords": [
    "tma-cloud",
    "electron",
    "desktop",
    "client"
  ],
  "author": "ZIDAN44",
  "license": "ISC",
  "main": "src/main/index.cjs",
  "dependencies": {
    "mime-types": "^3.0.2"
  },
  "scripts": {
    "start": "electron .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build:clouddrive": "node scripts/build-clouddrive.js",
    "build:client": "node scripts/prepare-client-build.js && node scripts/build-clouddrive.js && npx electron-builder --config src/build/electron-builder.client.json",
    "build:client:win": "node scripts/prepare-client-build.js && node scripts/build-clouddrive.js && set CSC_IDENTITY_AUTO_DISCOVERY=false&& npx electron-builder --config src/build/electron-builder.client.json --win",
    "build:client:win:unpacked": "node scripts/prepare-client-build.js && node scripts/build-clouddrive.js && set CSC_IDENTITY_AUTO_DISCOVERY=false&& npx electron-builder --config src/build/electron-builder.client.unpacked.json --win",
    "build:client:win:portable": "node scripts/prepare-client-build.js && node scripts/build-clouddrive.js && set CSC_IDENTITY_AUTO_DISCOVERY=false&& npx electron-builder --config src/build/electron-builder.client.portable.json --win",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.1.10",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3",
    "eslint": "^10.7.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "globals": "^17.7.0",
    "prettier": "^3.9.6",
    "vitest": "^4.1.10"
  }
}
