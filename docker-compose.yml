# Docker Compose Configuration for TMA Cloud
# Requires .env file with environment variables

services:
  # INIT SERVICE: Prepares filesystem permissions
  # Runs once, fixes ownership of ./uploads, and exits.
  init-permissions:
    image: busybox:latest
    container_name: tma-cloud-init
    # Create directory if missing -> change ownership to 1001:1001
    command: sh -c "mkdir -p /uploads && chown -R 1001:1001 /uploads"
    volumes:
      - ./uploads:/uploads

  # TMA Cloud App
  app:
    image: ghcr.io/tma-cloud/tma:latest
    container_name: tma-cloud-app
    restart: unless-stopped
    ports:
      - "${BPORT:-3000}:${BPORT:-3000}"
    env_file:
      - .env
    environment:
      - LOG_FORMAT=json
    volumes:
      - ./uploads:/app/uploads
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:${BPORT:-3000}/health || exit 1"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      init-permissions:
        condition: service_completed_successfully
    networks:
      - tma-cloud-network

  # PostgreSQL database
  postgres:
    image: postgres:18-alpine
    container_name: tma-cloud-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${DB_USER:-postgres}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-postgres}
      - POSTGRES_DB=${DB_NAME:-tma_storage}
      - POSTGRES_INITDB_ARGS=--encoding=UTF8 --locale=C
    #ports:
      #- "127.0.0.1:5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql
    networks:
      - tma-cloud-network
    command: >
      postgres
      -c shared_buffers=512MB
      -c work_mem=8MB
      -c maintenance_work_mem=128MB
      -c max_connections=100
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # Redis Cache
  redis:
    image: redis:latest
    container_name: tma-cloud-redis
    restart: unless-stopped
    command: >
      redis-server
      ${REDIS_PASSWORD:+--requirepass ${REDIS_PASSWORD}}
      --appendonly yes
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis-data:/data
    networks:
      - tma-cloud-network
    healthcheck:
      test: ["CMD-SHELL", "if [ -n \"$REDIS_PASSWORD\" ]; then redis-cli -a \"$REDIS_PASSWORD\" ping; else redis-cli ping; fi | grep -q PONG || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3

  # TMA Cloud Audit Worker
  worker:
    image: ghcr.io/tma-cloud/tma:latest
    container_name: tma-cloud-worker
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - LOG_FORMAT=json
    volumes:
      - ./uploads:/app/uploads
    command: node backend/audit-worker.js
    depends_on:
      app:
        condition: service_started
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      init-permissions:
        condition: service_completed_successfully
    networks:
      - tma-cloud-network

volumes:
  postgres-data:
    driver: local
  redis-data:
    driver: local

networks:
  tma-cloud-network:
    driver: bridge
